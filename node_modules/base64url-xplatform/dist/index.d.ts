/// <reference types="node" />
import { BufferShim } from 'buffer-esm';
export { BufferShim } from 'buffer-esm';
export declare class Base64Url {
    static encode(input: string | Buffer | ArrayBuffer, encoding?: 'utf8' | 'utf-8'): string;
    static decode(base64url: string, encoding?: 'utf8' | 'utf-8'): string;
    static toBase64(base64url: string | Buffer | ArrayBuffer): string;
    static fromBase64(base64?: string): string;
    static padBase64(input?: string): string;
    static toBuffer(base64url: string): BufferShim;
}
